import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "CMFA Registration | Coast Fashion and Modelling Awards 2026",
  description:
    "Register for complimentary in-house attendance at the Coast Fashion and Modelling Awards 2026. CMF executives, models, contestants, and sponsors receive approved QR tickets.",
};

export default function CmfaRegistrationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
