import { cardClassName } from "@/components/dashboard/ui";

/** Shared admin card — keep this import so visitor-management screens stay on the design system. */
export const VM_CARD = cardClassName("default");
